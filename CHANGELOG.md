# 更新日志

本文件记录"本地资源管理器"的版本变更历史。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.7] - 2026-08-06

### 新增
- **多音频根目录** — 音频模块支持同时配置多个根目录路径（设置对话框可添加/移除），每个根目录独立做目录级指纹预检与歌单清理：扫描根 A 不会再误删根 B 的数据。所有根目录扫描到的歌单直接混合显示在同一个歌单列表中。
- **配置迁移** — 旧版单路径 `AUDIO_ROOT` 配置自动迁移为 `AUDIO_ROOTS` 列表，无需手动修改 config.json。

## [v1.1.6] - 2026-08-06

### 修复
- **返回歌单列表时音乐被切断** — 原 `_open_playlist()` 每次导航到详情页都调用 `self.player.stop()` 并隐藏播放条，从子歌单返回上级/歌单列表时会再次触发而切断播放。

### 优化
- **持续播放** — 播放逻辑引入独立播放队列 `_play_queue`，与详情页展示列表解耦：双击某首曲目时以当前列表快照建立队列，上一首/下一首/暂停恢复均在独立队列上操作。返回歌单列表或切换详情页不再重置队列、不再停止播放。
- **当前曲目高亮** — 高亮改为按曲目路径匹配展示列表（`_highlight_current_in_list()`），切换回歌单后仍在播放的曲目会自动高亮。

### 重构
- **音频模块拆分** — 原 1600+ 行的 `audio_view.py` 拆分为 `audio_theme`（配色/工具）、`audio_widgets`（卡片/设置对话框）、`audio_browser`（歌单浏览器）、`audio_detail`（详情页）、`audio_player`（播放条）、`audio_threads`（后台线程）和精简后的主窗口，每个模块 400-600 行。
- **写真模块拆分** — 原 777 行的 `main_window.py` 拆分为 `photo_theme`（主题样式）、`photo_widgets`（设置对话框/扫描线程）和精简后的主窗口。

## [v1.1.5] - 2026-08-04

### 修复
- **扫描完成后 UI 更新延迟** — 原"扫描完成"进度回调发在容器歌单曲目数聚合和目录指纹重建之前，而 UI 刷新要等扫描线程返回（`finished` 信号）后才执行，导致用户看到"扫描完成"后列表还要等待数秒才更新。现在"扫描完成"回调移到所有耗时操作之后，聚合前先提示"正在整理曲目数..."，`finished` 信号发出时数据已就绪、UI 立即刷新。

### 优化
- **track_count 批量更新** — 新增 `update_track_counts_batch()`，容器歌单聚合后一次 `executemany` 批量写入，替代逐条 `update_playlist`（每次短连接 connect+commit+close），数百歌单场景耗时从数秒降到毫秒级。

## [v1.1.4] - 2026-08-03

### 新增
- **目录级指纹** — `fingerprint_cache` 新增 `level="dir"` 指纹级别：只 stat 目录 mtime（不 stat 文件），捕获文件夹/文件增删与重命名，速度比文件级指纹快一个数量级。写真模块和音频模块的扫描预检均切换到该级别。
- **指纹缓存格式 v2** — 缓存文件 `.scan_fingerprint.json` 升级为带 `version` 字段的 v2 格式，支持 `dir_fingerprint` / `file_fingerprint` 双级别共存；读取时对 v1 格式（只有 `fingerprint` 字段）做懒迁移，向后兼容。
- **自动扫描静默化** — 写真模块和音频模块进入程序时若目录无变化，扫描条不再出现，用户无感；仅当检测到真实进度（`current < total`）时才弹出扫描条。手动扫描行为不变。
- 新增 20+ 个单元测试：目录级指纹稳定性 / 增删捕获 / 漏检预期、dir 与 file level 独立性、v1→v2 缓存迁移、dir level 扫描集成（音频 + 写真模块）。

### 优化
- **unchanged 预检提速** — 800+ 文件夹场景下，音频模块扫描预检从十几秒（约 1.7 万次文件 `os.stat`）降到 1-2 秒（约 800 次目录 `os.stat`）。写真模块同理受益。
- 移除音频模块未使用的 `_FP_EXTENSIONS` 常量。

### 变更
- 写真模块和音频模块的指纹预检均从文件级（mtime + size）切换到目录级（目录 mtime）。
- 指纹缓存文件 `.scan_fingerprint.json` 结构升级（含 `version`、`dir_fingerprint` / `file_fingerprint`、`level` 字段）。

### 已知行为
- 升级到 v1.1.4 后**首次启动写真/音频模块仍会完整扫描一次**（旧缓存无 `dir_fingerprint`，触发重新扫描建立目录级指纹），之后才提速。
- 目录级指纹会漏检"文件内容修改"（如替换封面图 / 编辑标签文件内容 / 替换图片）——基于用户场景"仅增删文件夹与文件"的前提，此漏检可接受并已在 `fingerprint_cache.py` 顶部注释记录。

## [v1.1.3] - 2026-07-29

### 新增
- **复制歌单名称** — 右键歌单卡片或列表项可复制歌单名称到剪贴板。
- **标签按钮** — 详情页标签以可点击的胶囊按钮显示，点击即按该标签过滤。
- **标签选择器** — 导航栏 `+` 按钮弹出全量标签窗口，支持搜索过滤、多选，确定后按 AND 逻辑过滤。
- **标签叠加** — 多次从详情页或选择器添加标签时自动叠加过滤；导航栏显示标签芯片，可逐个 × 移除。

### 优化
- **递归 CTE 优化** — 后代歌单 / 曲目 / 封面查询改用递归 CTE，500+ 子歌单从 150+ 次查询降为 3 次。
- **翻页按需渲染** — 翻页仅构建当前可见视图（网格/列表），切换时按需重建，翻页耗时减半。
- **封面延迟加载** — PlaylistCard 封面通过 QTimer 异步加载，翻页时卡片瞬间渲染完成。
- **时间排序缓存** — 文件夹 mtime 写入数据库，排序时零文件 IO，旧数据库惰性迁移（后台线程批量补全）。
- **批量子歌单查询** — `get_child_counts_batch()` 一次查询替代 N 次逐个查询，翻页从 60+ 次降到 1 次。
- **批量删除重构** — `delete_playlist` 改用递归 CTE 批量删除，开启外键 CASCADE 自动清理曲目。
- **临时表方案** — `list_tracks_by_playlist_ids` / `delete_tracks_not_in` 改用临时表，突破 SQLite 999 参数上限。

## [v1.1.2] 及更早版本

更早的版本变更未在本文件中维护，详见 [README.md](README.md) 功能列表与 Git 提交历史。
